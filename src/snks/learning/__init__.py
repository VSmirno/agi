"""Stage 78b+: learned components that sit on top of the symbolic core.

This package holds residual learners, surprise accumulators, rule nurseries,
and other self-supervised modules that the agent uses to correct and extend
the textbook. Everything here is a *correction* over the symbolic base, never
a replacement for it.
"""
