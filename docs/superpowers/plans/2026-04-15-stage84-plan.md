# Stage 84 Implementation Plan — Real Stimuli Infrastructure

**Spec:** docs/superpowers/specs/2026-04-15-stage84-stimuli-layer-design.md
**Date:** 2026-04-15

## Task 1 — Archive diag files (blocker removal)

Rename both files before any code changes:
- `experiments/diag_stage83.py` → `experiments/_archived_diag_stage83.py`
- `experiments/diag_stage83_bugs.py` → `experiments/_archived_diag_stage83_bugs.py`

Verification: `ls experiments/_archived_diag_stage83*.py`

## Task 2 — Create `src/snks/agent/stimuli.py`

New file with:
- `Stimulus` base dataclass: `evaluate(trajectory) -> float`
- `SurvivalAversion(weight=1000.0)`: returns `-weight` if terminated, else 0.0
- `HomeostasisStimulus(vital_vars, weight=1.0)`: returns `weight * min(final.body[v])`
- `StimuliLayer(stimuli)`: returns `sum(s.evaluate(trajectory) for s in stimuli)`

All imports: `VectorTrajectory` from `vector_sim` (TYPE_CHECKING guard).

Verification: `from snks.agent.stimuli import StimuliLayer, SurvivalAversion, HomeostasisStimulus`

## Task 3 — Fix `vector_sim.py`: score_trajectory

Change signature: `vital_vars=None` → `stimuli: StimuliLayer | None = None`

New logic:
- `stimuli is not None` → `(stimuli.evaluate(trajectory), total_gain, -steps)` (3-tuple)
- `stimuli is None` → `(survived, total_gain, -steps)` (3-tuple, backward compat)

Remove `min_vital` from return. Keep `total_gain` and `survived` logic.

Verification: no import errors, function returns 3-tuple in both paths.

## Task 4 — Fix `vector_mpc_agent.py`: inv/body split (2 locations)

**Location 1 — step start (~line 449-450):**
```python
raw_inv = dict(info.get("inventory", {}))
VITAL_VARS = {"health", "food", "drink", "energy"}
body = {v: float(raw_inv.get(v, 9.0)) for v in vitals}
inv  = {k: v for k, v in raw_inv.items() if k not in VITAL_VARS}
```

**Location 2 — episode end (~line 677):**
```python
raw_inv_end = dict(info.get("inventory", {}))
body_at_end = {v: float(raw_inv_end.get(v, 0.0)) for v in vitals}
```

Verification: grep for `info.get(v, 9.0)` and `info.get(v, 0)` — must be gone.

## Task 5 — Fix `vector_mpc_agent.py`: wire StimuliLayer + tuple-splice

1. Add `stimuli: StimuliLayer | None = None` param to `run_vector_mpc_episode`
2. Default: create `StimuliLayer([SurvivalAversion(), HomeostasisStimulus()])` if `stimuli is None`
3. Pass `stimuli` to `score_trajectory(traj, stimuli=stimuli)` at scoring line (~573)
4. Update tuple-splice (~584):
   ```python
   score = (sim_score[0], known, gain, sim_score[2])  # (stimuli_score, known, gain, -steps)
   ```

Verification: no positional unpack of old 4-tuple remains.

## Task 6 — Update test files

Update `tests/test_vector_sim.py` and `tests/test_vector_mpc.py`:
- Replace `score_trajectory(traj, vitals)` → `score_trajectory(traj)` (uses stimuli=None path)
- Update any tuple-unpack that expects 4 elements → 3

Verification: `grep -n "score_trajectory" tests/test_vector_sim.py tests/test_vector_mpc.py`

## Task 7 — Write `tests/test_stimuli.py`

Unit tests:
- `test_survival_aversion_terminated`: score < 0 when terminated
- `test_survival_aversion_alive`: score == 0.0 when not terminated
- `test_homeostasis_min_vital`: returns min of vitals from final_state
- `test_homeostasis_empty`: returns 0.0 when no final_state
- `test_stimuli_layer_sum`: two stimuli → sum of their scores
- `test_score_trajectory_with_stimuli`: 3-tuple, position 0 is stimuli score
- `test_score_trajectory_without_stimuli`: stimuli=None → survived at position 0

## Task 8 — Write `tests/test_vital_fix.py`

Integration test using mock info dict:
- `info = {"inventory": {"food": 2.0, "health": 5.0, "wood": 3, "stone": 1}}`
- Parse with the same logic as vector_mpc_agent
- Assert `body["food"] == 2.0` (not 9.0)
- Assert `body["health"] == 5.0`
- Assert `"food" not in inv`
- Assert `"health" not in inv`
- Assert `inv["wood"] == 3`

## Task 9 — Run tests locally

```
.venv/bin/python -m pytest tests/test_stimuli.py tests/test_vital_fix.py -v
```

Fix any failures before proceeding.

## Task 10 — Update `experiments/stage83_vector_eval.py`

Update call to `run_vector_mpc_episode` — no tuple unpack changes needed there
(it calls the function, doesn't unpack score_trajectory directly).
Verify no `vital_vars` positional pass to `score_trajectory`.

## Task 11 — Write `experiments/stage84_eval.py`

Eval script for minipc with 4 gates:
- `sleep_at_low_energy`: force energy=2, check sleep chosen >80% of steps
- `no_sleep_at_full_energy`: energy=9, sleep chosen <5%
- `survival_ge_155`: 20 episodes, mean survival ≥ 155
- `wood_ge_10pct`: wood ≥3 in ≥10% of episodes

Save results to `_docs/stage84_eval.json`.

## Task 12 — Commit

```
git add src/snks/agent/stimuli.py src/snks/agent/vector_sim.py \
    src/snks/agent/vector_mpc_agent.py tests/test_stimuli.py \
    tests/test_vital_fix.py experiments/stage84_eval.py \
    experiments/_archived_diag_stage83.py experiments/_archived_diag_stage83_bugs.py
git commit -m "feat(stage84): Real Stimuli Infrastructure — vital fix + StimuliLayer"
```
